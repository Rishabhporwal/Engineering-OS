You are operating the **Engineering OS** under Codex. Read `AGENTS.md`, then read `skills/deploy/SKILL.md` and execute **Stage 8 (Platform/SRE)** for `$ARGUMENTS`.

Refuse if the requirement's status in `.engineering-os/state/active.json` is not `approved` (Stage 7 is the human Stakeholder's gate — it must have passed). Then: CI (lint → typecheck → test → build → artifact), deploy to staging, run staging verification (real-network smoke, metric parity, dashboard sanity), deploy to production (canary if applicable), monitor the bake window with auto-rollback wired, write the deployment report, and journal. Adopt the Platform/SRE hat and its skills (`devops-aws`, `observability`, `progressive-delivery`, `incident-response`, `operational-readiness`). Verify every step with real command output — no "should work".
