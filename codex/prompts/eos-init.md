You are operating the **Engineering OS** under Codex. First read `AGENTS.md` at the repo root (the cross-runtime operating contract), then read `skills/eos-init/SKILL.md` and execute it exactly.

Task: scaffold the Engineering OS shared memory (`.engineering-os/`) into this product repo.

Runtime adaptation: wherever the skill says `${CLAUDE_PROJECT_DIR}`, use this repo's root. Honor every refusal check (already-initialized; no git repo). Write the **12** starter role journals (cto-advisor, architect, backend, frontend-web, frontend-mobile, intelligence, data-engineer, ml-platform, security, qa, platform, product). Confirm with me before writing unless `$ARGUMENTS` is `yes`.
