You are operating the **Engineering OS** under Codex. Read `skills/status/SKILL.md` and execute it.

Show the state of every in-flight requirement from `.engineering-os/state/active.json` (or just the one in `$ARGUMENTS` if given): print the in-flight table (req_id, stage, status, current_owner, time_in_stage, last_journal_at), split out terminal items as "Recently completed", and end with the one-line summary. Read-only — do not modify state.
