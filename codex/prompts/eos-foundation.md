You are operating the **Engineering OS** under Codex. Read `AGENTS.md` at the repo root (the cross-runtime operating contract), then read `skills/foundation/SKILL.md` and execute it for the product brief in `$ARGUMENTS`.

> Mirrors the `foundation` command-skill: the agentic Foundation phase — draft the entire Product Canon (all 11 slots) from the brief + a repo scan, then the Stakeholder reviews and approves per file. Run once per product, after `/eos-init`.

Runtime adaptation: you have no Agent tool, so where the skill says "spawn the team in parallel," **adopt each drafting role's hat sequentially** (architect → security-reviewer → intelligence-engineer → platform-devops → cto-advisor), reading each role's `agents/<role>.md` before drafting its Canon files. Everything else is unchanged: batch the interview into ONE message (≤10 questions), mark every guess `> ASSUMPTION:`, present each file for explicit per-file approval (never batch-approve STACK/COMPLIANCE/TRIGGER-SURFACES silently), seal with the foundation sentinel, and commit `.engineering-os/knowledge-base/` with explicit paths. Where the skill says `${CLAUDE_PLUGIN_ROOT}`/`${CLAUDE_PROJECT_DIR}`, resolve to the installed OS dir / this repo's root.
