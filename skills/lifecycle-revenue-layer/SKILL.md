---
name: lifecycle-revenue-layer
description: RESET — the product's revenue/lifecycle engine. Previously encoded the prior business's RFM + audience builder + channel routers + compliance engine. Blanked for a new business plan; to be redefined once the business canon is re-fed.
---

# Revenue / Lifecycle Layer — RESET (awaiting new business plan)

> This skill previously held the product's **revenue engine** (the prior business: RFM scoring, the audience builder primitive, channel routers, AI-calling abstraction, compliance engine, recovered-revenue attribution). It was **cleared on a Founder reset**.
>
> **Do not assume a revenue/lifecycle model until this is re-fed.**

## To re-fill (once the business is defined)
If the new business has a revenue/lifecycle engine, define here:
- The core revenue primitive(s) and how they're built once + consumed N times (Single-Primitive Rule).
- Customer/segment model and how it drives action.
- Channels/actions and their routing (with compliance gates per the business canon).
- How outcomes are attributed and measured.

*Prior content retained in git history. Replace this skeleton with the new revenue/lifecycle design (or delete the skill if the new business has no such layer).*
